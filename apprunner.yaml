yaml:
  version: 1
  services:
    - name: tsc
      source: ./Backend/tsc/tsc  # Specify the path to your application folder
      build:
        command: mvn clean package  # Specify the build command